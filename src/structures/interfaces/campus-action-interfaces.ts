/**
 * Campus Action interface
 * @interface ICampusAction
 * @description This interface is used to define the campus action required data
 */
export default interface ICampusAction {
    url: string,
    token: string
}